<div class="row">
  <div class="col-lg-4">
    <div class="panel panel-danger">
      <div class="panel-heading">
        <h2 class="panel-title">Add Admin Permissions</h2>
      </div>

      <div class="panel-body">
        <p>Admin allows the user to basically do whatever the fuck they want. Be wise and don't give this privilege to anyone. </p>
        <%= form_tag add_permissions_path, method: :get, class:'col-lg-12' do%>
            <div class="input-group">
              <%= text_field_tag :add_admin, params[:add_admin], class: 'form-control', placeholder: 'Type email', type: "search",data: {autocomplete: autocomplete_user_email_pages_path }, 'min-length' => 1%>
              <span class="input-group-btn">
                  <%= submit_tag 'Go', name: nil, class: 'btn btn-primary' %>
                </span>
            </div>
        <%end%>
        <br>
      </div>

      <div class="card">
        <div class="card-header">
          <h4 class="card-title">Admin List</h4>
        </div>
        <table class="table card-table">
          <% @all_admins.each do |user| %>
            <tr>
              <td><%= user.full_name %></td>
              <td class="text-right"><%= link_to 'Remove', remove_permissions_path(user), class: 'btn btn-xs btn-danger'%></td>
            </tr>
          <% end %>
        </table>
      </div>

    </div>

  </div>

  <div class="col-lg-4">
    <div class="panel panel-info">
      <div class="panel-heading">
        <h2 class="panel-title">Add Organizer Permissions</h2>
      </div>
      <div class="panel-body">
        <p>Organizer allows the user to check other user's applications, checkout hardware and many other organizational tools.</p>
        <%= form_tag add_permissions_path, method: :get, class:'col-lg-12' do%>
        <link rel="stylesheet" href="//code.jquery.com/ui/1.12.1/themes/base/jquery-ui.css">
            <div class="input-group">
              <%= text_field_tag :add_organizer, params[:add_organizer], class: 'form-control', placeholder: 'Type email',
               data: {autocomplete: autocomplete_user_email_pages_path}, 'min-length' => 1%>
              <span class="input-group-btn">
                  <%= submit_tag 'Go', name: nil, class: 'btn btn-info' %>
                </span>
            </div>
        <%end%>
        <br>
      </div>

      <div class="card">
        <div class="card-header">
          <h4 class="card-title">Organizer List List</h4>
        </div>
        <table class="table card-table">
          <% @all_organizers.each do |user| %>
            <tr>
              <td><%= user.full_name %></td>
              <td class="text-right"><%= link_to 'Remove', remove_permissions_path(user), class: 'btn btn-xs btn-danger' %></td>
            </tr>
          <% end %>
        </table>
      </div>

    </div>
  </div>


  <div class="col-lg-4">
    <div class="panel panel-warning">
      <div class="panel-heading">
        <h2 class="panel-title">Add Mentor Permissions</h2>
      </div>
      <div class="panel-body">
        <p>Mentors get access to the mentorship dashboard that is exclusive for answering mentorship requests that are made by attendees </p>
        <%= form_tag add_permissions_path, method: :get, class:'col-lg-12' do%>
            <div class="input-group">
              <%= text_field_tag :add_mentor, params[:add_mentor], class: 'form-control', placeholder: 'Type email', data: {autocomplete: autocomplete_user_email_pages_path}, 'min-length' => 1 %>
              <span class="input-group-btn">
                  <%= submit_tag 'Go', name: nil, class: 'btn btn-warning'%>
                </span>
            </div>
        <%end%>
        <br>
      </div>


      <div class="card">
        <div class="card-header">
          <h4 class="card-title">Mentor List</h4>
        </div>
        <table class="table card-table">
          <% @all_mentors.each do |user| %>
            <tr>
              <td><%= user.full_name %></td>
              <td class="text-right"><%= link_to 'Remove', remove_permissions_path(user), class: 'btn btn-xs btn-danger'%></td>
            </tr>
          <% end %>
        </table>
      </div>


    </div>
  </div>
</div>
