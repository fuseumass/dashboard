<div class="card-body">

    <noscript>
        Hello <%= current_user.first_name %>. It appears that you have Javascript disabled in your browser. While that might be
        prudent on some websites, you do need it enabled for full judging functionality on Dashboard. <br>
        Our code is open source, so you should be able to see that we aren't hiding anything sketchy :)
    </noscript>

    <div class="col-lg-8">
        <% if @project.nil? %>
            <p>Error! Unable To Load Project!!</p>
        <% end %>

        <%= form_with(model: judgement, url: judging_assign_score_path(@judgement), local: true, method: :post) do |form| %>

        <% if judgement.errors.any? %>
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <strong>Unable to Judge Project.</strong>
                <% judgement.errors.full_messages.each do |message| %>
                <p><%= message %></p>
                <% end %>
            <button type="button" class="close" data-dismiss="alert" aria-label="Close"></button>
            </div>
        <% end %>

        <!--
        These are hidden fields used by the controller. Please don't touch them without checking the method of the judging controller! 
        -->

        <div class="form-group">
            <%= text_field_tag :project_id, "", class: 'form-control', value: @project.id, required: true, hidden: true %>
        </div>

        <!-- End of hidden fields here -->

        <div class="form-group">
            <%= label_tag :project_title %>
            <%= text_field_tag :project_title, "", class: 'form-control', value: @project.title, required: true, disabled: true %>
        </div>

        <!--
            TODO: Implement Dynamic Juding Fields Here!!
        -->

        <div class="actions">
            <%= form.submit "Judge Project", class: 'btn btn-primary' %>
        </div>
        <% end %>
    </div>

</div>
