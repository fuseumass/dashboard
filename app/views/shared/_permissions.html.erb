<div class="row">
  <div class="col-md-4 d-flex align-items-stretch">
    <div class="card">
      <div class="card-status bg-red"></div>
        <div class="card-header">
          <h3 class="card-title">Add Admin Permissions</h3>
        </div>
      <div class="card-body">
        <%= form_tag add_permissions_path, method: :post, class:'col-lg-12' do%>
            <div class="input-group">
              <%= text_field_tag :add_admin, params[:add_admin], class: 'form-control', placeholder: 'Type email', type: "search",data: {autocomplete: autocomplete_user_email_pages_path }, 'min-length' => 1%>
              <span class="input-group-btn">
                  <%= submit_tag 'Go', name: nil, class: 'btn btn-danger' %>
                </span>
            </div>
        <%end%>
      </div>
      <div class="card-footer">
        <p>Admin allows the user to basically do whatever they want. Be wise who you give this to.</p>
      </div>
    </div>
  </div>

  <div class="col-md-4 d-flex align-items-stretch">
    <div class="card">
      <div class="card-status bg-blue"></div>
      <div class="card-header">
        <h3 class="card-title">Add Organizer Permissions</h3>
      </div>
      <div class="card-body">
        <%= form_tag add_permissions_path, method: :post, class:'col-lg-12' do%>
            <div class="input-group">
              <%= text_field_tag :add_organizer, params[:add_organizer], class: 'form-control', placeholder: 'Type email',
                data: {autocomplete: autocomplete_user_email_pages_path}, 'min-length' => 1%>
              <span class="input-group-btn">
                  <%= submit_tag 'Go', name: nil, class: 'btn btn-blue' %>
                </span>
            </div>
        <% end %>
      </div>
      <div class="card-footer">
        <p>Organizer allows the user to check other user's registrations, checkout hardware and many other organizational tools.</p>
      </div>
    </div>
  </div>

  <div class="col-md-4 d-flex align-items-stretch">
    <div class="card">
      <div class="card-status bg-warning"></div>
      <div class="card-header">
        <h3 class="card-title">Add Mentor Permissions</h3>
      </div>
      <div class="card-body">
        <%= form_tag add_permissions_path, method: :post, class:'col-lg-12' do%>
          <div class="input-group">
            <%= text_field_tag :add_mentor, params[:add_mentor], class: 'form-control', placeholder: 'Type email', data: {autocomplete: autocomplete_user_email_pages_path}, 'min-length' => 1 %>
            <span class="input-group-btn">
                <%= submit_tag 'Go', name: nil, class: 'btn btn-warning'%>
              </span>
          </div>
        <% end %>
      </div>
      <div class="card-footer">
        <p>Mentors get access to answer mentorship requests that are made by attendees and judge assigned projects.</p>
      </div>
    </div>
  </div>

</div>

<div class="row">
  <div class="col-md-4 d-flex align-items-stretch">
    <div class="card">
      <div class="card-header">
        <h4 class="card-title">Admin List</h4>
      </div>
      <div class="card-body o-auto" style="height: 15rem">
        <table class="table card-table">
          <% @all_admins.each do |user| %>
            <tr>
              <td><%= user.full_name.titleize %></td>
              <td class="text-right">
              <%= form_tag remove_permissions_path(user), method: :post do%>
                <%= submit_tag "Remove", class: 'btn btn-xs btn-danger'%>
            <%end%>
              </td>
            </tr>
          <% end %>
        </table>
      </div>
    </div>
  </div>
  
  <div class="col-md-4 d-flex align-items-stretch">
    <div class="card">
      <div class="card-header">
        <h4 class="card-title">Organizer List</h4>
      </div>
      <div class="card-body o-auto" style="height: 15rem">
        <table class="table card-table">
          <% @all_organizers.each do |user| %>
            <tr>
              <td><%= user.full_name %></td>
              <td class="text-right"><%= form_tag remove_permissions_path(user), method: :post do%>
                <%= submit_tag "Remove", class: 'btn btn-xs btn-danger'%>
            <%end%></td>
            </tr>
          <% end %>
        </table>
      </div>
    </div>
  </div>

  <div class="col-md-4 d-flex align-items-stretch">
    <div class="card">
      <div class="card-header">
        <h4 class="card-title">Mentor List</h4>
      </div>
      <div class="card-body o-auto" style="height: 15rem">
        <table class="table card-table">
          <% @all_mentors.each do |user| %>
            <tr>
              <td><%= user.full_name %></td>
              <td class="text-right"><%= form_tag remove_permissions_path(user), method: :post do%>
                <%= submit_tag "Remove", class: 'btn btn-xs btn-danger'%>
            <%end%></td>
            </tr>
          <% end %>
        </table>
      </div>
    </div>
  </div>
</div>
